{{define "pair"}}

{{$name := concat (ttn .Elem) "Set"}}

type {{$name}} struct {
	types.Set[{{.Elem}}]
}

func New{{$name}}() *{{$name}} {
	v := new({{$name}})
	v.Set = *types.NewSet[{{.Elem}}](10)
	return v
}

{{end}}